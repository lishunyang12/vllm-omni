async_chunk: false

# Pipeline.__call__ requires guidance_scale (no default); 7.0 is the SD3 reference value.
stages:
  - stage_id: 0
    gpu_memory_utilization: 0.9
    devices: "0"
    enforce_eager: true
    default_sampling_params:
      seed: 42
      num_inference_steps: 28
      guidance_scale: 7.0
