-r common.txt
onnxruntime>=1.23.2
fa3-fwd==0.0.2
# FlashAttention-4 provides the FA-family path on Blackwell (sm100+).
# On sm80-90, fa3-fwd above remains the preferred path and FA4 is unused.
flash-attn>=4.0.0
# nvidia-cublas-cu12 is pinned to 12.9.1.4 via force-reinstall in Dockerfile.ci
# because torch pins its own cublas version, making it unsatisfiable at resolve time.
